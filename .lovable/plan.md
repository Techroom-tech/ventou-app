
# Système Email — Terminé

## Action requise

Exécuter le SQL ci-dessous dans **Supabase SQL Editor → New Query → Run**.

Les tables, RLS, et seed data seront créés. Ensuite, naviguer vers `/admin/settings/email` pour configurer un fournisseur.
